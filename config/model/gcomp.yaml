# @package _global_
#
# LTMLE (R `ltmle` + `SuperLearner`) CAPO pipeline configuration
# Used by `pipelines/train_ltmle.py`
#
model:
  name: gcomp
  method: gcomp

  # SuperLearner CV folds used inside ltmle
  v_folds: 3

exp:
  output_dir: './results/gcomp'


